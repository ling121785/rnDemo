import React, { Component } from 'react';
import { Text, View, StyleSheet, Dimensions } from 'react-native';

class plan extends Component {
    render() {
        return (
            <View class="style.planContainer">
                 <Text>home</Text>
            </View>
        );
    }
}

const style = StyleSheet.create({
    planContainer :{
        
    }
});

export default plan